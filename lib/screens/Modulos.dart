import 'package:flutter/material.dart';

class Modulos extends StatelessWidget {
  const Modulos({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Módulos'),
      ),
      body: const Center(
        child: Text('Pantalla de módulos (en blanco)'),
      ),
    );
  }
}